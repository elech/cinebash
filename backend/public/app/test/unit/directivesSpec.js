'use strict';

/* jasmine specs for directives go here */

describe('directives', function() {
  var $scope;
  beforeEach(module('myApp.directives'));
/*  beforeEach(inject(function($compile, $_rootScope_){
    $scope = $_rootScope_.$new();
  }));
*/
/*  describe('playlist', function() {
    it('should have many ', inject(function($compile, $rootScope){
*/      

    //  var scope = $rootScope.$new();
  //    scope.songs = [{title : 'dat name'}];
//      var element = $compile('<playlist></playlist>')(scope);

/*      scope.$digest();
      dump(element);*/
/*    }));

    it('should print current version', function() {
      module(function($provide) {
        $provide.value('version', 'TEST_VER');
      });
      inject(function($compile, $rootScope) {
        var element = $compile('<span app-version></span>')($rootScope);
        expect(element.text()).to.equal('TEST_VER');
      });
    });*/
 // });
});
